# Project memory

This is a compact index of durable project facts. Source, tests, approved plans,
and runtime behavior remain authoritative.

| Key | Durable fact | Evidence | Last verified |
|---|---|---|---|
| `delivery.g4.correction-child-dispatch` | G4's seventh offline slice requires a separate creator-signed exact order and one task/cycle-unique private claim before obtaining a child-signed proposal. The digest-matching approved plan, existing bound owned source, a separately signed read-only creator-test byte view, brief and criteria enter the offer; host and immutable no-mount worker validate replacement scope and reported allowance, then current Git is replayed. The clean installed-wheel smoke passed 1,834 tests, but the sandboxed aggregate source run was non-green. The earlier creator-test-suite digest remains an approval claim. The receipt grants no provider, host write, measured-spend or acceptance authority. A concrete model broker, write integration, final suites and independent review remain open. | `docs/G4_CORRECTION_CHILD_DISPATCH.md`; `docs/G4_CORRECTION_CHILD_DISPATCH_VERIFICATION.md`; `tests/test_reviewer_correction.py` | 2026-09-24 |
| `delivery.g4.bounded-correction` | G4's sixth offline slice adds schema-2 failed-test identities, exact two-run assertion reproduction, enrolled judge and creator signatures, two one-use correction-cycle claims with conservative aggregate allowances/deadline, and renewed custody/Git/candidate completion with unchanged approved plan/creator-test digests, reviewer-test bytes, adapter and static inputs. The clean installed-wheel smoke passed 1,832 tests. It grants no child/provider dispatch, repository write or acceptance. Critic quorum, actual creator-test Git blobs and spend are not authenticated by this boundary; production correction and final review remain open. | `docs/G4_BOUNDED_CORRECTION.md`; `docs/G4_BOUNDED_CORRECTION_VERIFICATION.md`; `tests/test_reviewer_correction.py` | 2026-09-24 |
| `delivery.g4.candidate-execution` | G4's fifth offline L2 slice separately authorizes one exact candidate reviewer-test request with a domain-signed creator approval, current authenticated provenance/control/Git replay, and an exclusive claim in one private persistent controller store. The generic isolated runner rejects candidate requests; the gated path uses the immutable no-mount offline image, revalidates source after execution and retains failed or passing count receipts without correction or acceptance authority. Local one-use protection depends on the chosen store and trusted same-UID host/clock; the later bounded-correction evidence gate consumes failed receipts, while coding-child/correction dispatch, final whole-suite review and production approval remain open. | `docs/G4_CANDIDATE_EXECUTION.md`; `docs/G4_CANDIDATE_EXECUTION_VERIFICATION.md`; `tests/test_reviewer_candidate_execution.py` | 2026-09-24 |
| `delivery.g4.authenticated-provenance` | G4's fourth offline L2 slice authenticates exact creator approval, blind reviewer custody, bounded path-disjoint child assignment/result, read-only Git reconstruction and prior known-control evidence with domain-separated Ed25519 artifacts. Fixed-argv Git replay verifies base/child/final ancestry, the full-index binary patch, owned changed paths, every bound blob and a clean worktree; a final `HEAD` check narrows revision races. Schema-2 single-operator mode discloses no independent human review and still requires a distinct child key. Its own downstream authority remains false; later candidate and correction-evidence gates consume this record. Physical identity/key custody, trusted external time, production correction dispatch, final whole-suite execution and independent review remain open. | `docs/G4_AUTHENTICATED_PROVENANCE.md`; `docs/G4_AUTHENTICATED_PROVENANCE_VERIFICATION.md`; `tests/test_reviewer_provenance.py` | 2026-09-24 |
| `delivery.g4.isolated-test-execution` | G4's third offline L2 slice executes one exact frozen reviewer package and bound source/resource tree only through the immutable-image no-mount container, with no host fallback. Canonical private receipts retain exact collected/started/executed/skipped and outcome counts plus ordered test-ID digests; paired controls require a successful known-good and assertion-only known-bad over distinct implementation trees but identical package/adapter/image/collection identities. Later authenticated-provenance and candidate-admission slices now bind these controls to custody/VCS/E2 evidence and one separately approved offline candidate run. The generic runner itself grants no candidate, correction or acceptance authority. | `docs/G4_ISOLATED_TEST_EXECUTION.md`; `docs/G4_ISOLATED_TEST_EXECUTION_VERIFICATION.md`; `tests/test_reviewer_test_execution.py` | 2026-09-24 |
| `delivery.g4.implementation-binding` | G4's second offline L2 slice creates an immutable record binding the canonical reviewer package before and after inspection to the exact source revision claim, complete regular-file inventory under declared source roots, dependency lock/build declarations and a declarative direct-symbol adapter. All reviewer Python files must use the exact adapter allowlist; wrappers, monkeypatches, result substitution/interception and downstream authority are false. The later isolated-count and authenticated-provenance slices now consume and connect this record to trusted Git, while dependency/image completeness and arbitrary dynamic semantic proof remain open. | `docs/G4_IMPLEMENTATION_BINDING.md`; `docs/G4_IMPLEMENTATION_BINDING_VERIFICATION.md`; `tests/test_reviewer_implementation_binding.py` | 2026-09-24 |
| `delivery.g4.test-package` | G4's first offline L2 slice freezes a complete blind reviewer-test package and content-addressed plan/interface/rubric/creator reference identities. The canonical artifact embeds every declared package byte and collection expectation, rejects unsafe aliases, unselected tests, vacuous execution and direct undeclared skip/xfail markers, and grants no execution, implementation-binding, write, VCS, credential, network, provider, correction or acceptance authority. It does not authenticate approval/independence or satisfy the now-separate binding record, isolated execution/count, known-good/known-bad, correction and final-review gates. | `docs/G4_REVIEWER_TEST_PACKAGE.md`; `docs/G4_REVIEWER_TEST_PACKAGE_VERIFICATION.md`; `tests/test_reviewer_test_package.py` | 2026-09-24 |
| `delivery.g3.feasibility` | G3 now has an offline fixed-matrix feasibility preflight plus metadata-only independent-label inventory and baseline/full-candidate/component-ablation sealing contracts. The plan-reference design remains infeasible at 27,712 cases and 498,816 assignments. Label eligibility requires matching Ed25519 claims from two distinct enrolled keys and known selection/observation probabilities; policy sealing fixes all required outcomes and retains unavailable ablations. No real signed G3 catalog, trust policy or reviewed arm/resource identities are present, so production eligibility remains zero and no production policy is sealed. G2's live read-only qualification dependency is now satisfied, but these remaining G3 inputs still deny execution, promotion, routing, utility or savings claims. | `docs/G3_FEASIBILITY_VERIFICATION.md`; `docs/G3_CONTEXT_STUDY.md`; `docs/LIVE_REVIEW_CLEANUP_BOUNDARY_QUALIFICATION_2026-09-24.md`; `tests/test_context_study.py` | 2026-09-24 |
| `delivery.next.live-review` | G2 live read-only qualification is complete for commit `3b32f1426ce067696307c3ea4a96ef61ea583411` and production image `sha256:3f67fa22ae6ede838269a292ad507e6441d6a5d084fbf5a2c6077ea125be8653`. Its sealed campaign produced three authenticated qualifying slots and three `accept` verdicts with no findings or required changes; one slot transparently tolerated one `invalid_evidence` critic under its threshold-two policy. All 32,226 micro-USD settled locally, no ledger entry is unresolved, all 12 workers were removed, and fresh reconstruction returned `accepted`. This closes the historical three-slot qualification gap but grants no launch, retry, routing, provider-authorship or billing authority. The campaign is time-bounded and is not retained as launch authority; any target live call requires a wholly separate current launch decision, ledger and exact gates. | `docs/LIVE_REVIEW_CLEANUP_BOUNDARY_QUALIFICATION_2026-09-24.md`; `docs/LIVE_READ_ONLY_REVIEW_QUALIFICATION_VERIFICATION.md`; `docs/REVIEW_LAUNCH_ADMISSION.md` | 2026-09-24 |
| `workflow.template-guidance` | Material work uses a task-appropriate production-template guide and filled verification/threat-model records before implementation. | `AGENTS.md`; `docs/PRODUCTION_TEMPLATE_SURVEY_2026-09-19.md` | 2026-09-19 |
| `provider.live-authority` | Offline previews, campaign seals, observer handoffs, evidence submissions, and conformance checks grant no dispatch authority. Live probes and launches require exact signatures, local approvals, current guidance, spending, runtime, and cleanup gates. Schema-1 `separated` mode requires disjoint human-role keys; ADR-0005 schema-2 `single_operator` mode permits one shared signer and explicitly accepts the absence of independent human review. | `docs/adr/0005-single-operator-review-authorization.md`; `docs/REVIEW_LAUNCH_ADMISSION.md`; `tests/test_review_launch_admission.py` | 2026-09-19 |

Do not add routine progress, transcripts, secrets, private data, or unsupported
inferences. Update an existing key rather than appending a duplicate fact.
